name: Validate Debugging Reference

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate repository hygiene
        shell: pwsh
        run: ./4.SharePoint-Migration/Tests/Validate-DebugOnlyRepository.ps1
